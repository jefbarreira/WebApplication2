name: Build WebApp2 App

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  workflow_call:
    inputs:
      base-version:
        required: true
        type: string

jobs:
  build:    
    uses: jefbarreira/nettest-build/.github/workflows/ci-generic-build-net.yml@main
    with:
      base-version: ${{ inputs.base-version }}
      runner: 'windows-net'
      repo: 'jefbarreira/WebApplication2'
      solution: '.\WebApplication2.sln'
